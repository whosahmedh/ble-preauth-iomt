cmake_minimum_required(VERSION 3.20.0)
find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(ble_preauth_iomt)

target_sources(app PRIVATE
    src/main.c
    src/auth_service.c
)

target_include_directories(app PRIVATE include)

target_sources(app PRIVATE
    src/main.c
    src/auth_service.c
    src/state_machine.c
)